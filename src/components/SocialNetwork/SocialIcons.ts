import styled from "styled-components";

export const SocialIcons = styled.div`
  display: flex;
  flex-direction: row;
  justify-content:center;
  width: 100%;
  svg {
    width: 26px;
    height: 26px;
    margin: 1rem 0.8rem;
    cursor: pointer;
  }
`